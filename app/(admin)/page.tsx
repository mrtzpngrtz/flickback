// This file is intentionally empty — route handled by app/page.tsx
// The (admin) route group is used for layout only via (admin)/layout.tsx
// which is not active in the current architecture.
export {}
